# UI primitives

Reusable, accessible UI primitives will live here beginning in Phase 1. shadcn/ui components should
be added individually as needed and adapted to the tokens in `docs/DESIGN_SYSTEM.md`.
